# Cloudflare Pages SPA fallback — serve index.html for client-side routes
# (existing static files like /assets/*, /sw.js, /manifest.webmanifest are
# served directly and are not affected by this rewrite).
/*    /index.html   200
